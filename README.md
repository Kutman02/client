# Driver's Music - Клиентское приложение

**from KutSoft**

## 📱 О проекте

**Driver's Music** — это веб-приложение для синхронизированного воспроизведения музыки в такси. Позволяет водителю и пассажирам совместно управлять плейлистом и слушать музыку в реальном времени.

### Основная концепция

- **Водитель** создает сессию, получает QR-код и управляет плейлистом через личный кабинет
- **Пассажиры** сканируют QR-код, вводят код доступа и подключаются к сессии водителя
- Все участники могут добавлять треки, управлять воспроизведением и видеть синхронизированный прогресс видео
- Изменения в плейлисте и состоянии плеера синхронизируются в реальном времени через WebSocket

## 🛠 Технологический стек

### Основные технологии

- **React 19.2.0** — UI библиотека для построения интерфейса
- **TypeScript 5.9.3** — типизированный JavaScript для надежности кода
- **Vite 7.2.4** — современный сборщик и dev-сервер с быстрой перезагрузкой
- **React Router DOM 7.11.0** — маршрутизация и навигация между страницами

### Управление состоянием

- **Redux Toolkit 2.11.2** — управление глобальным состоянием приложения
- **RTK Query** — мощный инструмент для работы с API и кеширования данных
- **React Redux 9.2.0** — интеграция Redux с React компонентами

### Стилизация

- **Tailwind CSS 4.1.18** — utility-first CSS фреймворк для быстрой стилизации
- **Lucide React 0.562.0** — современная библиотека иконок
- **React Icons 5.5.0** — дополнительные иконки

### Медиа и интерактивность

- **React Player 3.4.0** — универсальный плеер для видео/аудио
- **React YouTube 10.1.0** — интеграция с YouTube API
- **QRCode.react 4.2.0** — генерация QR-кодов для доступа пассажиров

### Реальное время

- **Socket.IO Client 4.8.1** — WebSocket клиент для синхронизации в реальном времени
  - Синхронизация прогресса видео
  - Обновления плейлиста
  - Управление воспроизведением
  - Подключение/отключение пассажиров

### Drag & Drop

- **@dnd-kit/core 6.3.1** — современная библиотека для drag and drop
- **@dnd-kit/sortable 10.0.0** — сортировка элементов
- **@hello-pangea/dnd 18.0.1** — альтернативная библиотека для перетаскивания

### HTTP клиент

- **Axios 1.13.2** — HTTP клиент для запросов к API

### Утилиты

- **clsx 2.1.1** — условное объединение классов
- **tailwind-merge 3.4.0** — умное объединение Tailwind классов
- **react-swipeable 7.0.2** — обработка жестов свайпа

### Инструменты разработки

- **ESLint 9.39.1** — линтер для проверки качества кода
- **@vitejs/plugin-react-swc 4.2.2** — плагин Vite с компилятором SWC для быстрой сборки
- **PostCSS 8.5.6** — обработка CSS
- **Autoprefixer 10.4.23** — автоматическое добавление вендорных префиксов

## 🏗 Архитектура приложения

### Структура проекта

```
client/
├── src/
│   ├── api/              # API слой (RTK Query, Socket.IO)
│   ├── components/       # React компоненты
│   │   ├── Cabinet/      # Компоненты личного кабинета водителя
│   │   └── Passenger/    # Компоненты интерфейса пассажира
│   ├── hooks/            # Кастомные React хуки
│   ├── pages/            # Страницы приложения
│   ├── redux/            # Redux store и слайсы
│   ├── types/            # TypeScript типы
│   ├── utils/            # Утилиты (кеширование, YouTube)
│   └── styles/           # Глобальные стили
```

### Основные страницы

- **Home** (`/`) — главная страница (landing page)
- **Auth** (`/auth`) — страница входа/регистрации
- **Cabinet** (`/cabinet`) — личный кабинет водителя с плеером и QR-кодом
- **Passenger** (`/passenger/:username`) — интерфейс пассажира
- **Thanks** (`/thanks`) — страница благодарности

### Ключевые функции

#### Для водителя:
- Создание и управление плейлистом
- Генерация QR-кода для доступа пассажиров
- Управление воспроизведением (play/pause, перемотка, переключение треков)
- Просмотр списка подключенных пассажиров
- Возможность исключить пассажира из сессии
- Поиск и добавление треков с YouTube

#### Для пассажира:
- Подключение к сессии водителя через QR-код и код доступа
- Просмотр текущего трека и плейлиста
- Управление воспроизведением (синхронизировано с водителем)
- Добавление треков в очередь
- Управление порядком треков (drag & drop)
- Удаление треков из плейлиста

### Синхронизация в реальном времени

Приложение использует **Socket.IO** для синхронизации:
- Прогресс воспроизведения видео (обновляется каждые 500ms)
- Состояние плеера (playing/paused)
- Изменения в плейлисте (добавление, удаление, перемещение треков)
- Переключение текущего трека
- Подключение/отключение пассажиров

### Кеширование

Локальное кеширование данных через `localStorage`:
- Плейлист
- Состояние плеера
- Прогресс видео
- Код доступа

## 🚀 Запуск проекта

### Установка зависимостей

```bash
npm install
```

### Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173` (или другому порту, указанному Vite)

### Сборка для production

```bash
npm run build
```

### Предпросмотр production сборки

```bash
npm run preview
```

### Линтинг

```bash
npm run lint
```

## 📝 Особенности реализации

- **Типобезопасность** — полная типизация на TypeScript
- **Оптимистичные обновления** — мгновенная реакция UI на действия пользователя
- **Автоматическое кеширование** — RTK Query автоматически кеширует запросы
- **Офлайн поддержка** — данные сохраняются в localStorage
- **Адаптивный дизайн** — работает на мобильных и десктопных устройствах
- **Темная тема** — современный темный интерфейс

## 🔐 Безопасность

- Аутентификация через JWT токены
- Коды доступа для пассажиров (обновляются каждые 5 минут)
- Валидация доступа на сервере
- Защита от несанкционированного доступа к сессиям

## 📦 Зависимости сервера

Приложение работает с backend сервером, который должен быть запущен на `http://localhost:5001`

## 🎨 UI/UX

- Современный минималистичный дизайн
- Плавные анимации и переходы
- Интуитивно понятный интерфейс
- Визуальная обратная связь на все действия пользователя

